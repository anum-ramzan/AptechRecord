@model IEnumerable<AptechRecord.Models.usp_select_consolidated_attendance_Result>

@{
    ViewBag.Title = "Consolidated Attendance";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section contentHeader{
    <h1>
        Attendance
        <small></small>
    </h1>
    <ol class="breadcrumb">
        <li><a href=@Url.Action("Dashboard", "Admin")><i class="fa fa-dashboard"></i>Dashboard</a></li>
        <li><a href=@Url.Action("Index", "Attendances")><i class="fa fa-list"></i>Attendance</a></li>
        <li>Consolidated Attendance</li>
    </ol>
}
<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">Consolidated Attendance for @ViewBag.Days</h3>
            </div>
            <div class="box-body">
                <table id="ConsolidatedAttendancetable" class="table">
                    <thead>
                        <tr>
                            <th>
                                Faculty Name
                            </th>
                            <th>
                                Batch Code
                            </th>
                            <th>
                                Timing
                            </th>
                            <th>
                                Head Count
                            </th>
                            <th>
                                Attendance %
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.EmployeeName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.BatchCode)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Slot)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Head_Count)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Attendance)
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th colspan="3" style="text-align:center;font-weight: 700;">Grand Total</th>
                            <th></th>
                            <th></th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
<script>
    $(document).ready(function () {
        $('.dataTables_empty').html('<b>Waiting for attendance (Attendance have not uploaded yet) .............</b>')
    });
</script>
